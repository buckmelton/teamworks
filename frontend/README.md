# Frontend (React + TypeScript + Vite)

## Setup
```bash
cd frontend
npm install
cp .env.example .env
```

## Run
```bash
npm run dev
```

The app runs on `http://localhost:5173` and calls the backend using `VITE_API_BASE_URL`.
